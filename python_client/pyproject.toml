[project]
name = "se7en-pro-client"
version = "1.0.1"
description = "Python Windows client for Se7en Pro based on psiphon-tunnel-core"
requires-python = ">=3.11"
dependencies = ["PySide6>=6.7,<7"]

[project.scripts]
se7en-pro = "se7en_pro.app:main"

[tool.pyinstaller]
name = "Se7enPro"
